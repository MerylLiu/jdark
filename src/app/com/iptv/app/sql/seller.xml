<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="seller">
	<select id="getSellers" resultType="java.util.HashMap">
		SELECT a.* FROM biz_seller a LIMIT ${offset},${rows}
	</select>

	<select id="getSellersCount" resultType="java.lang.Integer">
		SELECT count(*) as Count FROM biz_seller a
	</select>

	<select id="getExistCount" resultType="java.lang.Integer">
		SELECT count(*) as Count FROM biz_seller a WHERE a.Code = #{Code}
	</select>	

	<insert id="saveSeller" parameterType="java.util.HashMap">
		INSERT INTO biz_seller
			(Code, Name, SellerId, Tel, Address, Province, City, Area, IsInstall, SetUpDate, CreateDate, OrderNum)
		VALUES
			(#{Code}, #{Name}, #{SellerId}, #{Tel}, #{Address}, #{Province}, #{City}, #{Area}, 
			 #{IsInstall}, #{SetUpDate}, #{CreateDate}, #{OrderNum})
	</insert>

	<update id="updateSeller" parameterType="java.util.HashMap">
		UPDATE biz_seller SET Code = #{Code}, Name = #{Name}, SellerId = #{SellerId},
			Tel = #{Tel}, Address = #{Address}, Province = #{Province}, City = #{City}, Area = #{Area},
			IsInstall = #{IsInstall}, SetUpDate = #{SetUpDate}, OrderNum = #{OrderNum}
		WHERE Id = #{Id}
	</update>

	<delete id="deleteSeller" parameterType="java.util.HashMap">
		DELETE FROM biz_category WHERE Id = #{Id}
	</delete>
</mapper>