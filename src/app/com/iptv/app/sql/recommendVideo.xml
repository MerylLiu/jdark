<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="recommendVideo">

	<select id="getVideoPaged" resultType="java.util.HashMap">
		SELECT a.Id as Id,b.Id as VideoId,b.Code, b.Name, b.Jianpin, b.Quanpin,b.Subhead,b.AppVideoUrl,b.AppImageUrl,b.Cost, b.CostFZ, 
		b.VideoUrl, b.StartDate, b.EndDate, b.Style, b.CategoryId, b.Description,
		b.ImageUrl, b.GifUrl, b.QrCode, b.Province, b.City, b.Area, b.Provider, b.Maker, 
		b.Status, b.CreateDate, b.SellerKeyId, b.OrderNum,b.PlayTime,c.SellerId  
		FROM biz_recommend_video a 
		LEFT JOIN biz_video b ON a.VideoId = b.Id 
		LEFT JOIN biz_seller c ON b.SellerKeyId = c.Id
	</select>
	
	<select id="getVideoList" resultType="java.util.HashMap">
		SELECT VideoId as Id
		FROM biz_recommend_video
	</select>
	
	<insert id="saveVideo">
		INSERT INTO biz_recommend_video (VideoId) values(#{Id})
	</insert>

	<update id="deleteVideo" parameterType="java.util.HashMap">
		DELETE FROM biz_recommend_video 
		WHERE Id IN
		 <foreach collection="Id" index="index" item="item" open="(" separator="," close=")">  
            #{item}   
		 </foreach>
	</update>
</mapper>