<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="region">
	<select id="getRegions" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT a.Name as text,cast(a.Id as char) as value
		FROM sys_region a
	</select>

	<select id="getRegionsByParentId" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT a.Name as text,cast(a.Id as char) as value
		FROM sys_region a 
		WHERE a.ParentId = #{parentId}
	</select>

	<select id="getProvince" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT a.* FROM sys_region a LIMIT ${offset},${rows}
	</select>

	<select id="getCity" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT a.* FROM sys_region a LIMIT ${offset},${rows}
	</select>

	<select id="getCounty" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT a.* FROM sys_region a LIMIT ${offset},${rows}
	</select>
</mapper>