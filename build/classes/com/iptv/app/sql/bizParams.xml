<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bizParams">
	<select id="getBizParamsPaged" resultType="java.util.HashMap">
		SELECT * FROM biz_params ORDER BY KeyCode, OrderNum
	</select>
	
	<select id="findParam" resultType="java.util.HashMap">
		SELECT * FROM biz_params WHERE KeyCode = #{KeyCode} AND KeyValue = #{KeyValue}
	</select>
	
	<insert id="save"  parameterType="java.util.HashMap">
		INSERT INTO biz_params (KeyCode,KeyValue,Text,IsEnable,IsVisible,OrderNum,Remark) 
		VALUES (#{KeyCode},#{KeyValue},#{Text},#{IsEnable},#{IsVisible},#{OrderNum},#{Remark})
	</insert>
	
	<update id="update" parameterType="java.util.HashMap">
		UPDATE biz_params SET KeyCode = #{KeyCode}, KeyValue = #{KeyValue}, Text = #{Text}, IsEnable = #{IsEnable},
			IsVisible = #{IsVisible}, OrderNum = #{OrderNum}, Remark=#{Remark}
		WHERE KeyCode = #{BeforeKey} AND KeyValue = #{BeforeValue}
	</update>
	
	<delete id="delete" parameterType="java.util.HashMap">
		DELETE FROM biz_params WHERE KeyCode = #{KeyCode} AND KeyValue = #{KeyValue}
	</delete> 

	<select id="getBizDic" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT a.Text as text,a.KeyValue as value,a.*
		FROM biz_params a
		WHERE a.KeyCode = #{Key} AND a.isVisible = 1
		ORDER BY OrderNum
	</select>
</mapper>