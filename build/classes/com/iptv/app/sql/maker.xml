<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="maker">
	<select id="getMakersPaged" resultType="java.util.HashMap">
		SELECT a.* FROM biz_maker a
		WHERE (a.IsDel IS NULL OR a.IsDel = 0)
		ORDER BY a.OrderNum,a.CreateDate DESC
	</select>

	<select id="getMakerById" resultType="java.util.HashMap">
		SELECT a.* FROM biz_maker a Where a.Id = #{Id}
	</select>

	<select id="getExistCount" resultType="java.lang.Integer">
		SELECT count(*) as Count FROM biz_maker a
		WHERE a.Code = #{Code} AND (a.IsDel IS NULL OR a.IsDel = 0)
	</select>	

	<insert id="saveMaker" parameterType="java.util.HashMap">
		INSERT INTO biz_maker
			(Code, Name, SellerId, Tel, Address, Province, City, Area, CreateDate, OrderNum)
		VALUES
			(#{Code}, #{Name}, #{SellerId}, #{Tel}, #{Address}, #{Province}, #{City}, #{Area}, 
			 #{CreateDate}, #{OrderNum})
	</insert>

	<update id="updateMaker" parameterType="java.util.HashMap">
		UPDATE biz_maker SET Code = #{Code}, Name = #{Name}, SellerId = #{SellerId},
			Tel = #{Tel}, Address = #{Address}, Province = #{Province}, City = #{City},
			Area = #{Area}, OrderNum = #{OrderNum}
		WHERE Id = #{Id}
	</update>

	<delete id="deleteMaker" parameterType="java.util.HashMap">
		UPDATE biz_maker SET IsDel = 1 
		WHERE Id IN
		 <foreach collection="Id" index="index" item="item" open="(" separator="," close=")">  
            #{item}   
		</foreach>
	</delete>

	<select id="getMakersOptions" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT a.Name as text,a.Id as value
		FROM biz_maker a
		WHERE (a.IsDel IS NULL OR a.IsDel = 0)
		<if test="text != null and text != ''">
			 AND a.Name LIKE CONCAT('%',#{text},'%')
		</if>
	</select>
</mapper>