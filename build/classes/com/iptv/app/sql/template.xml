<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="template">
	<select id="getTemplatePaged" resultType="java.util.HashMap">
		SELECT * FROM biz_template ORDER BY CreateDate DESC
	</select>

	<select id="getTemplateById" resultType="java.util.HashMap">
		SELECT a.* FROM biz_template a Where a.Id = #{Id} 
	</select>

	<insert id="saveTemplate" parameterType="java.util.HashMap">
		INSERT INTO biz_template
			(Name, Content, ImageUrl,CreateDate)
		VALUES
			(#{Name}, #{Content},#{ImageUrl},#{CreateDate})
	</insert>

	<update id="updateTemplate" parameterType="java.util.HashMap">
		UPDATE biz_template SET Name = #{Name},Content = #{Content}
		WHERE Id = #{Id}
	</update>

	<delete id="deleteTemplate" parameterType="java.util.HashMap">
		DELETE FROM biz_template
		WHERE Id IN
		<foreach collection="Id" index="index" item="item" open="(" separator="," close=")">  
            #{item}   
		</foreach>
	</delete>
</mapper>